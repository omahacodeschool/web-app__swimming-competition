Here is information about each competitor:

<p>
<% @competitors.each do |c| %>
  The competitor <%= c.first_name %>
  <%= c.last_name%>
  from <%= c.school_name %> (<%= c.conference_name %> conference)
  has id # <%= c.id %>. - 
  <a href= "/delete/competitor_deleted/<%= c.id %>"> Delete competitor</a> - 
  <a href= "/update/competitor_update_form/<%= c.id %>"> Update competitor</a>
  <br>
<% end %>
</p>