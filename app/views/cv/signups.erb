<h1>Event Registration</h1>

<form action="/newsignup" method="post">
  <fieldset>
    <legend>Register for Events</legend>
    <select name = "swimmerid">
      <option>Select a Kitty...</option>
      <% @swimmers.each do |post| %>
        <option value="<%= post.id %>"><%= post.swimmer_name %>
        </option>
      <% end%>
    </select>  
    <br>
    Events:<br>
    <% @events.each do |event| %>
      <input type="checkbox" name="events[]" value="<%= event.id %>">
      <label><%= event.event_name %> 
    <% end %>
      <input type="submit">
  </fieldset>
</form>

<h2>Events:<h2>
<ul>
  <% @events.each do |post| %>
    <li><%=post.event_name%></li>
  <% end %>
</ul>

<a href="/">Home</a>